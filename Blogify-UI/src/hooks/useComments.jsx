import createAxiosInstance from "../api/axiosInstance"

const useComments = (id) => {
  const deleteComment = ()=>{
    try {
        
    } catch (error) {
        
    }
  }
}

export default useComments